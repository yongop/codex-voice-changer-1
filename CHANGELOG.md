# Changelog

이 프로젝트는 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을
따르며, 버전은 [Semantic Versioning](https://semver.org/lang/ko/)을
사용합니다.

## [Unreleased]

### Added

- 앱 최초 실행 시 공식 RVC 모델 이용규약 링크와 명시적 동의 화면
- 현재 검토 버전 기준의 로컬 동의 기록과 이전 버전 무효화
- 메인 화면의 `설정 순서` 카드: 권한 요청 허가 → 파일 접근 허가 →
  ChatGPT 음성 대화 켜기 → 음성 분리 켜기 → RVC 켜기 순서를 번호로 안내하고,
  각 단계에 마우스를 올리면 상세 설명이, 클릭하면 그 설명이 고정되어 표시
- 스피커 사용 시 변환 음성이 마이크로 되돌아가 대화 상대 앱이 자기 목소리를
  인식하는 문제의 해결책으로 macOS 음성 분리(마이크 모드) 안내
- 파일 접근 권한 설정을 바로 여는 버튼

### Changed

- 기존 `권한 요청 안내` 카드를 단계별 `설정 순서` 카드로 대체
- 창 높이를 고정값 대신 내용에 맞춰 결정하도록 변경 (최소 555pt)
- 변환 출력이 첫 블록 직후가 아니라 두 블록(0.44초)이 채워진 뒤
  시작되도록 프라이밍을 도입해, 블록별 추론 시간 변동을 흡수하는
  지터 여유를 상시 확보
- HuBERT 컨텍스트 창을 2.5초에서 1.2초로 줄여 블록당 추론 시간과
  변동 폭을 절반 가까이 축소
- 원음 폴백과 변환 재개 시 폴백 구간만큼 변환 프레임을 건너뛰어
  변환·원음 타임라인을 항상 일치시키고, 언더런 후에도 확보된 여유가
  유지되도록 개선
- 짧은 언더런(0.12초)마다 스트림 전체를 리셋하던 동작을 제거하고,
  단시간 내 2회 반복되는 끊김 또는 1.5초 연속 언더런에서만 스트림을
  재구성하도록 완화 (재구성할 때마다 프라이밍 깊이를 한 블록씩,
  최대 5블록까지 확대)
- 예상값 대신 실측한 파이프라인 지연으로 원음 폴백의 presentation
  time을 정렬
- 안정화 워밍업과 첫 라이브 추론 시간을 기준으로 2–5블록 사이의 지터
  버퍼를 자동 선택하고, 언더런 뒤에는 같은 전체 여유가 복구될 때까지
  변환 재개를 미뤄 원음·변환음이 짧게 반복 교차하는 현상을 억제
- 실제로 재생하지 않은 20ms 예약 시간을 지연 계산에서 제거해 변환음과
  원음 폴백의 크로스페이드 타임라인을 더 정확히 정렬
- `-80 dBFS` 이하의 Codex 무음 구간은 두 블록의 자연스러운 꼬리를 보존한
  뒤 신경망 추론을 건너뛰고, 고정 MPS/호스트 버퍼와 단일 inter-op
  스케줄러를 사용해 Apple Silicon의 지속 부하와 할당을 절감
- 실시간 PCM 링의 샘플별 복사를 연속 메모리 복사로 교체하고 Python
  작업자 통신 버퍼를 재사용해 오디오 콜백과 장시간 실행의 CPU·메모리
  오버헤드를 축소
- 런타임 설정 명령에서 이용규약 동의 인자를 제거하고 동의 시점을 앱
  최초 실행으로 이동
- 모델 ZIP의 비 UTF-8 파일명과 무관하게 체크포인트를 SHA-256으로 선택
- File Provider 폴더에서도 임시 경로에서 앱을 서명해 ZIP으로 보존하고
  `/Applications` 설치본을 별도로 검증

## [1.0.0] - 2026-07-27

### Added

- 특정 macOS 앱의 출력만 캡처하는 Process Tap
- 츠쿠요미짱 공식 RVC `通常1` 실시간 MPS 변환
- 원음 폴백, 크로스페이드와 장기 언더런 복구
- 재현 가능한 공식 모델·RVC 런타임 설정 및 SHA-256 검증
- 공개 저장소용 라이선스, 제3자 고지, 개인정보·보안·기여 문서
